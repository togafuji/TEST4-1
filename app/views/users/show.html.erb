<div class = "bodytitle">
<h2>スケジュール詳細</h2>
</div>
 
 <table>
   <tr>
     <th>タイトル</th>
     <td><%= @user.title %></td>
   </tr>
   <tr>
     <th>開始日</th>
     <td><%= @user.start_at.strftime('%Y年%m月%d日')%></td>
   </tr>
   <tr>
     <th>投稿日</th>
     <td><%= @user.updated_at.strftime('%Y年%m月%d日')%></td>
   </tr>
   <tr>
     <th>終日</th>
     <td><% if @user.alltime == true %>◯<% else %><% end %></td>
   </tr>
   <tr>
     <th>スケジュールメモ</th>
     <td><%= @user.onememo %></td>
   </tr>
 </table>
 
 <div>
   <ul>
     <li><%= link_to "スケジュール一覧に戻る", :users %></li>
     <li><%= link_to "スケジュールを編集する", "/users/#{@user.id}/edit" %></li>
     <li><%= button_to "スケジュールを削除する", user_path(@user.id), method: :delete, form: { data: { turbo_confirm: "本当に削除しますか" } } %></li>
   </ul>
 </div>